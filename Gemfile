source 'https://rubygems.org'

gemspec

# These are the latest versions from 2015, around the time that partitioned was
# last maintained and when the Jora version of the gem was forked from.
gem 'pg', '< 0.19'
gem 'bulk_data_methods', '< 1.2'

group :development do
  gem 'jquery-rails', '< 3.2'
  gem 'rspec-rails', '< 3.5'
end

gem 'activerecord-redshift-adapter', :git => 'https://github.com/arp/activerecord-redshift-adapter', :branch => 'rails4-compatibility'
